# Single-replica in-cluster Redis. A real deployment should use a managed
# Redis (ElastiCache/Memorystore) or the Bitnami/redis-operator chart for
# persistence and failover — this is the demo/dev-cluster version.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: sentinellm
spec:
  replicas: 1
  selector:
    matchLabels: { app: redis }
  template:
    metadata:
      labels: { app: redis }
    spec:
      containers:
        - name: redis
          # pinned by digest — re-resolve with
          # `docker pull redis:7-alpine && docker inspect --format='{{index .RepoDigests 0}}' redis:7-alpine`
          image: redis:7-alpine@sha256:ff02b58f971e7d7d156a1267e283fcbbeee91773b6aa36c49dac28ecfe28eadf
          ports: [{ containerPort: 6379 }]
          readinessProbe:
            exec: { command: ["redis-cli", "ping"] }
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            requests: { cpu: "50m", memory: "64Mi" }
            limits: { cpu: "200m", memory: "256Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: sentinellm
spec:
  selector: { app: redis }
  ports: [{ port: 6379, targetPort: 6379 }]
